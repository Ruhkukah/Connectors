scenario_id: add_order_reject_validation
description: AddOrder rejected by structural validation
metadata:
  version: 1
  deterministic_seed: 0
command_flow:
  - command: AddOrder
    expected_outcome: ValidationFailed
    live_send: false
confirmation:
  emits_fake_replication: true
  commit_bounded: true
  projected_private_state: true
