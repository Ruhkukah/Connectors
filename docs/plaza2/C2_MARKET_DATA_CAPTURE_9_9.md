# C2 market-data-only capture scenario — NOT EXECUTED

The executable and binary/import contract are prepared in
[C2_CAPTURE_HARNESS_9_9.md](C2_CAPTURE_HARNESS_9_9.md). This remains an unexecuted
T1 scenario requiring separate explicit authorization.

Status: **REQUIRES_T1_CAPTURE**, with a separate **BLOCKED** request for written identity authority. This document grants no
T1 access. PRs #38–42 and the C2 preflight remain unmerged. No publisher object, reply listener, trade host or order commands
belong in this capture. Use a listener-only harness in a later expressly authorized session.

## Inputs to obtain before capture

Use the existing qualified SDK/scheme hashes from `spec-lock/test/plaza2/public99/wire.json`. No new FTP file is currently
identified as necessary. Request MOEX's applicable written specification/clarification for:

1. The smallest `public_order_id` uniqueness domain, including regular versus multileg; numeric reuse after life, clearing,
   trading-day change and service restart; and permitted same-ID instrument/side/price/session changes.
2. Add operation amount versus remaining amount, whether equality is guaranteed, and cancellation quantity/remainder rules.
3. Whether the anonymous composite exposes `info` and all transaction callbacks; how completed snapshot validity is witnessed
   if `info` is internal; multiple-publication handling; and exact snapshot/log LifeNum relationship.
4. Supported multileg composite configuration, bound field and coordination. Do not infer atomicity between separate listeners.
5. Public-book meaning of relevant snapshot/log ClearDeleted and nonzero `replAct`, including MAX revision and repeated history.

A finite capture cannot replace the universal identity answers. Attach the exchange reply/document revision to the source lock
before freezing the production key. Capture credentials and endpoints must stay out of committed evidence.

## Two logical-pair candidates

Start with the documented regular pair:

```text
p2ordbook://FORTS_ORDLOG_REPL;snapshot=FORTS_ORDBOOK_REPL;name=c2_regular
```

The separate multileg candidate is:

```text
p2ordbook://FORTS_ORDLOG_REPL;snapshot=FORTS_ORDBOOK_REPL;name=c2_multileg;
snapshot.data=multileg_orders;online.data=multileg_orders_log;snapshot.bind=info.trades_rev
```

The second URL is shown wrapped for readability. Use the verified absolute `online.scheme`/`snapshot.scheme` paths if an
explicit client scheme is needed. Open with empty settings; do not inject C1 replstate. Confirm MOEX's supported configuration
before replacing this candidate with a different topology. Give each logical pair its own ordinal namespace and local epoch.

## Receipt requirements

For every OPEN, record SDK/runtime/source hashes, client settings with credentials removed, native scheme features, every
message ordinal/name/id/size, every field name/type/offset/size, and the complete descriptor lists. Preserve unknown service
tables. Fail recording explicitly on buffer capacity exhaustion; never silently drop callbacks.

Record strictly ordered callback sequence numbers per listener, connection/listener state transitions, local monotonic receive
time, poll boundaries, raw payload/null bytes and native callback identity. Capture TN_BEGIN/TN_COMMIT if exposed, ONLINE,
LifeNum with flags, ClearDeleted table/revision/flags, REPLSTATE if delivered, CLOSE reason and runtime errors. Preserve
`replID/replRev/replAct`, `infoID`, bound/life/publication, both status masks and public initial snapshot amount/time.

Store an immutable binary trace plus decoded index/field manifest and hashes. Map by each OPEN descriptor, not source ordinal.
A three-table synthetic fixture must not be relabeled as captured negotiation. Import the actual trace into the existing fake
shared-library driver, including extra tables and the observed control order; write assertions from the captured descriptors.

## Exercises and acceptance

| Exercise | Evidence and acceptance |
| --- | --- |
| Regular bootstrap | Complete negotiated mapping; usable snapshot witness; bound life/revision; ONLINE and transaction chronology |
| Multileg bootstrap | Exact supported topology, mapping, price field and bound; independent clocks/frontiers unless atomicity documented |
| Passive normal activity | Existing public orders' additions, partial/full fills, cancellations and pre-bound continuations; no activity generated by this harness |
| Partial publication / multiple info | Observe naturally or obtain official replay; identify complete publication without a guessed row order |
| Disconnect before snapshot / before ONLINE / after Ready | Loss immediately removes health; fresh snapshot and bound continuation reproduce final active state |
| LifeNum at six model positions | Import available observed controls; inject timing permutations offline; exchange LifeNum exercise only if separately coordinated |
| Relevant ClearDeleted, including MAX | Resolve historical row compaction versus active snapshot reconstruction independently for each mapped table |
| History/retention-equivalent failure | Record actual error/control sequence if obtainable; otherwise remain REQUIRES_T1_CAPTURE, no guessed downtime/window |
| Continuous versus fresh bootstrap | Per-pair canonical state equality at a shared documented committed frontier, including pre-bound surviving IDs |

Capture quiet/missing event categories as NOT OBSERVED, not passed. A later capture may still leave gates BLOCKED. Freeze only
what the evidence supports, rerun the offline oracle against imported fixtures, then present the gate verdict for a new
production-C2 authorization. Do not start the projector or expose public bindings during capture.
