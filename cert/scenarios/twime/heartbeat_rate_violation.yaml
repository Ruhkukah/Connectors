scenario_id: twime_heartbeat_rate_violation
title: Synthetic TWIME heartbeat rate violation
metadata:
  owner: phase2b1
  mode: fake_transport
steps:
  - step_id: establish
    action: connect and inject EstablishmentAck
    expected: session becomes Active
  - step_id: send_four_heartbeats
    action: send four client Sequence heartbeats within one second
    expected: fake session emits HeartbeatRateViolation and faults
